<div class="min-h-screen bg-gradient-to-br from-gray-900 to-gray-800 text-white p-6">
  <div class="bg-gray-700 p-6 rounded-lg shadow-lg max-w-2xl mx-auto">
    <h1 class="text-2xl font-bold mb-4">Post Details</h1>
    <p><strong>Content:</strong> <%= @post.content %></p>
    <p><strong>Scheduled At:</strong> <%= l @post.scheduled_at, format: :long %></p>
    <p><strong>Attachment:</strong> <% @post.attachment.each do |file| %>
      <%= link_to file[:filename], rails_blob_path(file), target: "_blank" %> <br>
    <% end if @post.attachment.present? %></p>
    <%= link_to 'Back', posts_path, class: "px-4 py-2 bg-blue-600 hover:bg-blue-700 rounded text-white mt-4 inline-block" %>
  </div>
</div>